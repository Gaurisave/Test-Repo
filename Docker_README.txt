# Build docker file
$ docker build - < Dockerfile

# List image that is newly created.
$ docker images

# select your newly created iamge and run docker using that. In following command centos is the image name.
$ docker run -it centos bash

